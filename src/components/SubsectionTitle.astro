---
---
<h4 class="container">
    <slot />
</h4>
<style>
    .container {
        position: relative;
        padding: 0 0 0 40px;
        margin: 0 0 24px;
        font-size: 28px;
    }
    .container::before {
        content: "";
        position: absolute;
        top: 2px;
        left: 0;
        width: 32px;
        height: 32px;
        background: var(--color-orange-primary);
        border: 1px solid var(255,255,255,0.5);
        border-radius: 12px;
        box-shadow: var(--shadow-primary-small);
    }
</style>